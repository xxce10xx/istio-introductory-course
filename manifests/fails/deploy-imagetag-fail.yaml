apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-violations
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo
  template:
    metadata:
      labels:
        app: demo
    spec:
      containers:
        - name: nginx
          image: nginx:latest       # Esto genera violación por :latest
          # Sin resources.limits
