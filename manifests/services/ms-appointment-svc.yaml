apiVersion: v1
kind: Service
metadata:
  name: ms-appointments
  namespace: ordendev
  labels:
    app: ms-appointments
spec:
  type: ClusterIP
  selector:
    app: ms-appointments
  ports:
    - name: http
      port: 8080
      targetPort: 3000
